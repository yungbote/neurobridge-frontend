This folder should contain the MediaPipe Face Mesh runtime assets required by WebGazer.

Populate it by running:
  ./scripts/fetch-mediapipe-face-mesh.sh

Assets are served at /mediapipe/face_mesh/* so WebGazer can load them from a stable base path.
